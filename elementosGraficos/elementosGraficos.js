const elementosGraficos = {
    cabecalhoTitulo: function(titulo) {
        console.log(`======================================================= \n`);
        console.log(`${titulo} \n`);
    },

    espaçamento: function () {
        console.log(``)
    }
}

module.exports = elementosGraficos;